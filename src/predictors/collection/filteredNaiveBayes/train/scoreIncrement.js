const way = require('senseway')
const findIncrements = require('./findIncrements')

module.exports = (slices, .., increment) => {
  const len = way.len(slices[0])

  for each slice
    const condition = slice[condChan][len - 1]
    infer(priors, fields, subset, slice as context)
    update truePos, falsePos, trueNeg, falseNeg

  const recall = fn1(truePos, falseNeg etc)
  const precision = fn2(truePos, falseNeg etc)

  const f1score = fn3(recall, precision)
  return f1score
}
